# This file is a stale artifact from an earlier src-layout attempt.
# The active package root is the project root directory.
# pyproject.toml maps: unified_url_toolkit = "." (see [tool.setuptools.package-dir])
# The authoritative __init__.py is the root-level __init__.py.
# This file is NOT used during install or import resolution.

__version__ = "0.1.0"
